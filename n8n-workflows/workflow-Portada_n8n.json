{
  "name": "Portada Diaria",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 5 0 * * *"
            }
          ]
        }
      },
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.3,
      "position": [
        0,
        0
      ],
      "id": "8caedb2e-a649-45c3-89a4-5faf145e28a5",
      "name": "Schedule Trigger"
    },
    {
      "parameters": {
        "jsCode": "const fs = require('fs');\n\nconst date = new Date();\ndate.setDate(date.getDate() - 8);\ndate.setFullYear(date.getFullYear() - 100);\nconst filename = date.toISOString().split('T')[0];\n\nconst path = `/home/kripta/apps/sansofe/static/portada/${filename}.json`;\nif (fs.existsSync(path)) fs.unlinkSync(path);\n\nreturn $input.all();\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        608,
        0
      ],
      "id": "7e564369-f83b-4760-bd05-6a4730980ee0",
      "name": "Elimina el JSON de 8 días atrás"
    },
    {
      "parameters": {
        "jsCode": "\nconst fs = require('fs');\n\nconst date = new Date();\ndate.setDate(date.getDate() + 2);\ndate.setFullYear(date.getFullYear() - 100);\nconst filename = date.toISOString().split('T')[0];\n\nconst articulos = $input.all().map(item => item.json);\nif (!articulos || articulos.length === 0) return [];\n\nconst SECCIONES = [\n  'anuncios', 'sucesos', 'sociedad', 'política', 'internacional',\n  'economía', 'cultura', 'deportes', 'religión', 'agricultura', 'militar', 'otros'\n];\n\nconst grouped = {};\nfor (const art of articulos) {\n  const topics = art.topics || [];\n  const seccion = topics.find(t => SECCIONES.includes(t)) || 'otros';\n  if (!grouped[seccion]) grouped[seccion] = [];\n  grouped[seccion].push(art);\n}\n\nconst dir = '/home/kripta/apps/sansofe/static/portada';\nfs.mkdirSync(dir, { recursive: true });\nfs.writeFileSync(`${dir}/${filename}.json`, JSON.stringify(grouped));\n\nreturn $input.all();\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        400,
        0
      ],
      "id": "7c940d78-f41d-403d-bee3-cdea4d186dd0",
      "name": "Genera el JSON de PORTADA para dentro de 2 días"
    },
    {
      "parameters": {
        "url": "=http://127.0.0.1:3002/portada?fecha={{ $now.plus({days: 2}).minus({years: 100}).toFormat('yyyy-MM-dd') }}",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        192,
        0
      ],
      "id": "33745fcc-8e00-433e-b9ed-fab33ee4d634",
      "name": "Llamada a la API única"
    }
  ],
  "pinData": {},
  "connections": {
    "Schedule Trigger": {
      "main": [
        [
          {
            "node": "Llamada a la API única",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Genera el JSON de PORTADA para dentro de 2 días": {
      "main": [
        [
          {
            "node": "Elimina el JSON de 8 días atrás",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Llamada a la API única": {
      "main": [
        [
          {
            "node": "Genera el JSON de PORTADA para dentro de 2 días",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate"
  },
  "versionId": "4a488390-de42-400f-8e30-4a68a02227c4",
  "meta": {
    "instanceId": "Oculto por seguridad"
  },
  "id": "5Jx987oWNq8RlAYd",
  "tags": []
}